# Week 13 - Managing Application Data

Builds on week 12's async Pokemon fetcher. Added `List<T>` to store Pokemon, `DateTime` to track fetch times, and an `Enum` for Pokemon types.

## How to Run

```bash
cd week-13-managing-application-data
dotnet run
```
