# Week 3 - Arrays

A C# console application demonstrating array initialization and iteration using `foreach` loops.

## What It Does

This program creates an array of string values containing names of classic sodas, then iterates over the array using a `foreach` loop to print each item to the console.

## How to Run

```bash
cd week-3-arrays
dotnet run
```
